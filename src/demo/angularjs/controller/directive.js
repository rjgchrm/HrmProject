require(['app'], function (app){
  app.registerController('hrm.controller.d', [
    function() {
      var _init, vm;
      vm = this;
      _init = function() {
      };
      _init();
    }
  ]);
});
